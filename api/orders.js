const {db}=require('./_lib');module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'method'});try{const b=req.body||{};if(!/^\d{17}$/.test(b.requestNo||'')||!/^\d{3}$/.test(b.requestCode||''))return res.status(400).json({error:'validation'});const {error}=await db().from('orders').insert({product:b.product,name:b.name,phone:b.phone,city:b.city,district:b.district,address:b.address,request_no:b.requestNo,request_date:b.requestDate,request_code:b.requestCode});if(error)throw error;return res.json({ok:true})}catch(e){return res.status(500).json({error:'save'})}}